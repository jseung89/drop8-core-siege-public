<!-- DROP8_REFACTOR_028_RECURRING_WEREWOLF_RITUAL_CYCLE -->
## Refactor 028 — Recurring Werewolf Ritual Cycle

- Open Arena 전용 반복 늑대인간 의식 사이클을 추가했다.
- 첫 예고 90초, 예고 15초, 활성 45초, 쿨다운 120초 상수로 관리한다.
- 기존 늑대인간 제단/저주/변신/은빛 무기함 구조를 재사용한다.
- 인간 플레이어만 Open Arena 반복 의식을 완료할 수 있고 AI는 기존 전투/루팅 행동을 유지한다.
- Refactor 027 보급상자 기능은 변경하지 않는다.
- Battle Royale 늑대인간·보급·비행기·자기장·전리품·라운드 로직은 보존한다.
- 무기 데미지, 발사속도, 이동속도, AI 판단 주기, 서버 Tick, 네트워크 주기는 변경하지 않았다.

<!-- DROP8_REFACTOR_027_OPEN_ARENA_RECURRING_SUPPLY_DROPS -->
## Refactor 027 — Open Arena Recurring Supply Drops

- Open Arena 전용 반복 보급상자 사이클을 추가했습니다.
- 첫 보급은 상시 전장 라운드 시작 후 60초, 이후 보급은 90초 간격으로 시도합니다.
- 활성 보급상자는 최대 3개로 제한하며, 최대치에 도달하면 해당 주기의 새 보급을 스킵합니다.
- 보급 위치 선정은 기존 보급 위치 검증과 지형·충돌·건물·플레이어 이격 검사를 재사용합니다.
- Battle Royale의 기존 단일 보급, 비행기, 자기장, 전리품 흐름은 유지합니다.
- 라운드 정산/리셋 중에는 새 보급이 발생하지 않으며, 다음 라운드 시작 시 타이머가 다시 예약됩니다.
- 무기 데미지, 발사속도, 이동속도, AI 판단 주기, 서버 Tick, 네트워크 주기는 변경하지 않았습니다.

<!-- DROP8_REFACTOR_025A_OPEN_ARENA_HUD_LAYOUT_HOTFIX -->
## Refactor 025A — Open Arena HUD Layout Hotfix

- Open Arena 전투 HUD에 'open-arena-layout' 클래스를 부여해 Battle Royale과 별도 레이아웃만 적용합니다.
- 실시간 순위표를 미니맵 우측 상단 영역에서 분리해 좌측 상단 독립 패널로 배치하고, 작은 화면에서는 높이와 행 정보를 축약합니다.
- 미니맵은 기존 우측 영역을 유지하며, Open Arena에서는 자기장 원·다음 원·안전구역 라벨을 그리지 않습니다.
- Open Arena의 자기장 DOM 항목과 Phaser 자기장 경고음은 숨기되, Battle Royale 자기장 HUD·월드 원·미니맵 라벨은 기존 경로를 유지합니다.
- 킬피드는 미니맵 아래로 내려 충돌을 줄이고, 확장 지도 상태에서는 전투 화면 침범을 피하도록 숨깁니다.
- 중앙 정산창은 독립 모달 계층으로 유지하며 킬 제한·정산·라운드 재시작 로직은 변경하지 않았습니다.

<!-- DROP8_REFACTOR_025_OPEN_ARENA_KILL_LIMIT_MATCH_CYCLE -->
## Refactor 025 — Open Arena Kill Limit & Match Cycle

- Open Arena 방 생성 시 10킬·20킬·30킬·무제한을 선택하며 서버가 허용값만 정규화합니다.
- 인간만 목표 킬과 정산 순위에 참여하고, 인간이 AI를 처치한 킬도 목표에 포함합니다. AI는 실시간·정산 순위에서 제외됩니다.
- 목표 달성 후 총 8초 동안 결과와 3초 카운트다운을 표시한 뒤 같은 방·방장·AI 설정을 유지하고 다음 라운드를 시작합니다.
- 정산 중 공격·피해·AI·리스폰을 정지하고 이전 라운드 타이머와 임시 월드 객체를 초기화합니다.
- Battle Royale의 마지막 생존자 승리와 결과 화면은 변경하지 않습니다.

<!-- DROP8_REFACTOR_024E_OPEN_ARENA_SCOREBOARD_UX -->
## Refactor 024E — Open Arena Scoreboard & UX

- Open Arena 전용 킬·데스·K/D·현재 및 최고 연속 처치·인간/AI 처치 통계를 서버 내부 Map으로 집계합니다.
- 기존 64필드 Schema 한도를 건드리지 않고 1초 저빈도 `arenaScoreboard` 메시지로 최대 20개 행을 전달합니다.
- HUD에 인간 정원, 살아 있는 AI/설정 슬롯, 상위 8명 순위와 리스폰·스폰 보호 상태를 표시합니다.
- Battle Royale 자기장·생존 HUD와 결과 흐름은 기존 동작을 유지합니다.

<!-- DROP8_REFACTOR_024D_OPEN_ARENA_PERSISTENT_WORLD -->
## Refactor 024D — Persistent Loot, Vehicle Respawn & World Cleanup

- Open Arena 최초 아이템 배치를 고정 슬롯으로 추적하고 획득 후 종류별 쿨다운으로 다시 생성합니다.
- 사망 시 장착 무기·제한 탄약·회복 1개만 드롭하며 동적 드롭은 60초 후 제거합니다.
- 파괴된 오토바이는 원래 슬롯에서 75초 뒤 안전 위치를 확인하고 재생성합니다.
- 1초 간격 정리 루프로 고아 드롭·예약·차량 슬롯을 관리하며 방 dispose 시 모든 런타임 Map을 비웁니다.

<!-- DROP8_REFACTOR_024C_OPEN_ARENA_RESPAWN -->
## Refactor 024C — Human & AI Respawn

- Open Arena 인간은 사망 후 5초 관전 뒤 안전 위치에서 권총 기본 장비로 재투입됩니다.
- 설정된 AI는 고정 슬롯과 동일한 이름·페르소나를 유지하며 6초 뒤 리스폰합니다.
- 최근 사용 지점, 적 거리와 직접 사격선을 평가하는 공통 안전 스폰 선택기를 추가했습니다.
- 리스폰 보호는 최대 2초이며 사격·근접·투척·트랩·상호작용 시 즉시 해제됩니다.
- Open Arena 사망 시 전체 인벤토리 드롭은 비활성화했으며 제한 드롭은 024D에서 추가합니다.

<!-- DROP8_REFACTOR_024B_OPEN_ARENA_LIFECYCLE_HOST_MIGRATION -->
## Refactor 024B — Lifecycle, Join-in-Progress & Host Migration

- 진행 중인 Open Arena에 인간 정원이 남아 있으면 서버 권위로 난입할 수 있습니다.
- 방장이 퇴장하면 명시적인 입장 시각 기준으로 가장 오래 접속한 인간에게 방장이 승계됩니다.
- 마지막 인간이 나가면 방을 즉시 비공개로 전환하고 10초 후 명시적으로 dispose합니다.
- Open Arena만 `autoDispose=false`를 사용하며 Battle Royale의 기존 방 수명은 유지합니다.

<!-- DROP8_REFACTOR_024A_OPEN_ARENA_FOUNDATION -->
## Refactor 024A — Open Arena Mode Foundation

- 신규 `openArena` 모드를 기존 Battle Royale과 분리했습니다.
- 방 생성 시 인간 최대 인원과 AI 인원을 별도로 검증하며 총 전투원은 16명으로 제한합니다.
- Open Arena는 비행기·낙하·자기장·마지막 생존자 종료 없이 전장에 바로 배치됩니다.
- 방 목록 metadata와 클라이언트 UI에 모드·인간 정원·AI 인원을 표시합니다.
- 진행 중 난입, 방장 승계, 반복 리스폰, 영구 아이템은 후속 024B~024E에서 단계적으로 추가합니다.

# 패치 노트

## Refactor 023 — AI Safe-Zone Sweep & Live Spectator Dialogue

<!-- DROP8_REFACTOR_023_AI_SAFE_ZONE_SWEEP_LIVE_SPECTATOR_DIALOGUE -->
- 공격 대상이 없는 AI는 매 판단마다 무작위 좌표를 다시 뽑지 않고, 자기장 진행 방향과 AI별 수색 섹터를 반영한 장기 `SAFE_SWEEP` 목적지를 유지합니다.
- 자기장 단계·중심이 바뀌거나 목적지 도착·만료·실패가 발생할 때만 다음 수색 구간을 생성해 짧은 왕복과 방향 흔들림을 줄였습니다.
- 수색 중 아이템은 진행 경로와의 거리 및 추가 우회 거리를 계산해 필요한 물품만 잠깐 획득하고, 완료 후 기존 수색 목표로 복귀합니다.
- 사망한 관전자에게도 새로운 AI 대사 이벤트를 전달하되 `loggable:false`로 보내 화면 밖 대사가 왼쪽 채팅 로그에 노출되지 않게 했습니다.
- 관전 말풍선은 더 이상 마지막 문장을 영구 보존하지 않으며 서버의 `durationMs` 동안만 표시되고 만료 후 사라집니다.
- 관전 중에는 현재 카메라 안에 있고 기존 캐릭터 가시성 규칙을 통과한 AI의 새 말풍선만 표시합니다.
- Refactor 021 페르소나 대사 데이터·발화 빈도, Refactor 022 수영 탈출·강 횡단·정체 복구, 기존 수동 무기·차량 밸런스를 유지합니다.

## Refactor 014 — Plane Visibility, Bazooka & Weapon Slot Swap

<!-- DROP8_REFACTOR_014_PLANE_VISIBILITY_BAZOOKA_SLOT_SWAP -->
- 비행기 탑승·낙하 단계에서는 모든 건물 지붕과 실내 가림막을 닫고, 전리품과 창문 오버레이를 표시하지 않습니다. 착지 후 실제 실내 공간에 들어간 경우에만 Refactor 013H1의 중첩 방 reveal 규칙을 적용합니다.
- 서버 권위 `RocketState`와 바주카포 폭발 판정을 추가했습니다. 벽·맵 경계·깊은 물·플레이어·오토바이 충돌, 거리 감쇠, 자기 피해, AI 운용, 재장전·드롭·사망 전리품을 기존 무기 체계에 연결했습니다.
- 오토바이 성능 상수는 유지하며, 최대 체력 180의 정상 오토바이는 직격 피해 170을 받아 10 HP(약 5.6%)가 남습니다. 반경 끝 피해는 직격보다 낮습니다.
- 주무기 1·2 슬롯 드래그 교환을 서버가 검증하고 동기화합니다. 클릭 선택과 우클릭 버리기는 유지하며 빈 슬롯 교환도 허용합니다. 탄창과 예비 탄약은 무기별 서버 상태에 남아 복제되거나 유실되지 않습니다.
- 폭발 상태에 위치·반경·공격자·무기 종류·차량 피해·구조물 피해·건물 ID를 포함했습니다. 이번 패치에서는 구조물을 실제로 파괴하지 않습니다.

### 확인된 원인

- 공중 플레이어의 월드 좌표가 건물 reveal 판정에 그대로 들어가 비행기가 건물 위를 지날 때 실내로 잘못 인식됐습니다.
- 기존 총알 상태는 단발 충돌 후 폭발 반경 판정과 향후 구조물 피해 메타데이터를 표현할 수 없었습니다.
- 무기 슬롯 UI는 선택 메시지만 보냈고, 서버에는 슬롯 순서를 원자적으로 교환하는 권위 메시지가 없었습니다.

## Refactor 012A — Smoke, Motorcycle Throwable & AI Navigation Recovery Hotfix

<!-- DROP8_REFACTOR_012A_SMOKE_MOTORCYCLE_AI_NAV -->
- 클라이언트 연막 판정에 서버 초 단위 시각을 그대로 전달해 연막이 즉시 만료된 것으로 보이던 오류를 수정했습니다.
- 서버가 `isDriving`, `vehicleId`, 오토바이 `driverId`와 파괴 상태를 재검증한 뒤 정상 운전자에게 투척을 허용합니다.
- 오토바이 투척의 시작 위치와 건물 공간은 서버 권위 차량 상태를 사용하며 차량 속도는 투척물 속도에 계승하지 않습니다.
- AI 경로 그래프에 넘을 수 있는 창문의 안쪽·바깥쪽 접근점을 추가하고 기존 서버 권위 창문 넘기 작업을 재사용합니다.
- AI가 막혔을 때 정면·사선·측면·후퇴 후보를 평가하고, 실패한 창문을 잠시 제외한 뒤 제한 주기로 경로를 다시 계산합니다.
- 화염 회피도 빈 경로로 직접 밀지 않고 정상 경로 계산을 사용합니다. 연막은 물리 장애물로 취급하지 않습니다.
- AI가 벽을 통과하거나 건물 사이를 순간이동하도록 충돌 규칙을 완화하지 않았습니다.

### 확인된 원인

- 기존 AI visibility graph는 건물 문과 모서리만 노드로 사용해 창문을 출입 경로로 선택할 수 없었습니다.
- 창문 넘기 진입점은 인간 클라이언트 메시지 전용이었고 `p.ai`를 명시적으로 거부했습니다.
- 지역 회피가 정면과 좌우 90도에 치우쳐 창틀·복합 장애물에서 같은 방향을 반복하기 쉬웠습니다.
- 화염·수류탄 회피는 목적지만 바꾸고 경로를 비워 건물 내부에서 직선 충돌을 반복할 수 있었습니다.

## 0.2.0 소스 전용 배포

- `.cmd`, `.bat`, `.ps1`, 사전 빌드 JavaScript 제거
- `npm run dev` 단일 포트 개발 실행
- `npm run dev:lan` LAN 주소 자동 표시
- Vite 8, Vitest 4 기반으로 갱신
- 전체 npm 보안 감사 취약점 0개
- Vitest 기반 Colyseus 통합 테스트
- 소스 ZIP 금지 확장자 검사 추가

## 후속 우선순위

1. AI 경로 탐색과 엄폐 개선
2. 클라이언트 예측·서버 재조정 고도화
3. 실제 인벤토리 드롭 UI
4. 효과음과 애니메이션
5. 공개 인터넷 서버 배포 검증


## refactor_001 - 렌더링·건물·아이템·전투 표현 개선
- 정적 맵 1회 렌더링, 동적 객체 화면 컬링, 좌표 보간, 미니맵 10Hz 갱신
- 건물 벽을 문이 있는 4면 충돌 구조로 변경
- 아이템별 선·점·도형 아이콘 및 근처 E 획득 안내 추가
- 주먹 양팔/펀치 애니메이션 및 총기별 실루엣 추가
- 근접무기 데이터와 서버 판정 오류 수정
- AI 판단 주기 완화 및 아이템 벽 겹침 방지

## refactor_002 — AI 지속 이동, 피격감, 넉백, 클라이언트 부드러움

- AI 판단 주기와 실제 이동 주기를 분리해 매 서버 틱마다 이동하도록 변경
- AI 벽 충돌 시 좌우 우회와 막힘 감지 추가
- 주먹·근접무기·총기별 서버 권위 넉백 추가
- hitSeq, lastHitAngle, lastHitDamage 상태 추가
- 피격 시 캐릭터 점멸, 충격 링, 카메라 흔들림, 화면 붉은 오버레이 추가
- 내 캐릭터 로컬 이동 예측과 서버 위치 완만 보정 추가
- 자기장·아이템을 10Hz 느린 레이어로 분리
- FPS 텍스트를 2Hz로 제한하고 만료 총알 표시 캐시 제거
- AI 이동 및 주먹 넉백 통합 테스트 추가

## refactor_003 — 회복, AI 장비 판단, 인게임 방 코드

- 4번 키를 자동 회복으로 변경하고 붕대/구급상자를 체력 손실에 따라 선택
- 회복 아이템은 완료 시점에만 차감하며 이동·공격·피격·무기 교체 시 취소
- 회복 종류와 진행률을 서버 상태 및 HUD에 표시
- 총기별 탄창을 분리해 무기 교체 후 탄창이 섞이는 문제 수정
- AI가 거리·탄약·탄창을 고려해 무기를 선택하고 빈 탄창을 재장전
- AI가 체력이 낮고 안전할 때 회복약 사용
- AI 파밍을 무기 업그레이드, 필요한 탄약, 방어구, 회복약 점수제로 변경
- 최근 총소리 조사, 마지막 목격 위치 기억, 저체력 후퇴 추가
- 건물 안팎 목표에 문 바깥/안쪽 경유점을 사용하는 이동 경로 추가
- 게임 중 상단 HUD에 방 코드를 항상 표시하고 클릭 시 초대 링크 복사
- 회복, 총기별 탄창, AI 무기 선택, AI 회복 통합 테스트 추가

## refactor_003 hotfix — AI weapon test and building navigation

- Fixed a false-negative AI weapon test that checked the equipped weapon after the rifle magazine had already been emptied and the AI had correctly switched to a pistol.
- Added deterministic isolation for AI combat tests.
- Added stable AI destination routes that are recalculated only when the goal changes or the AI is actually stuck.
- Added lightweight visibility-graph routing around relevant building corners and door points.
- Added sub-stepped collision movement to reduce corner tunneling and wall wedging.
- Added emergency relocation when an AI is already overlapping a wall.
- Added a building-door integration test that verifies an AI can route from the opposite side of a building, enter through its door, and pick up a weapon.

## Refactor 004A

- AI 착지 직후 파밍 우선 및 전투 준비도 적용
- 맨손 AI의 도주·비상 방어 행동 추가
- 서버 전용 AI 아이템 예약과 만료/해제 처리
- 아이템 스폰 간격, 문·벽 이격, 사망 드롭 분산
- 폐공장·주택가·병원·창고·군사시설·숲 캠프별 loot table
- 하단 슬롯형 인벤토리 HUD 및 Tab 상세 인벤토리
- 바닥 아이템과 현재 장비의 화력·사거리·연사력 비교 표시

### 알려진 제한

- 부쉬 및 지역별 장식은 Refactor 004B 범위
- 병원 치료대 등 지역 고유 상호작용 미구현
- AI 경로 탐색은 건물 문·모서리 경유 기반이며 전체 격자 A*는 아님
- 인벤토리 드래그 앤 드롭과 버리기 UI 미구현


## Refactor 004B — 지역 개성 및 부쉬 은폐

- 여섯 지역에 고유 바닥·벽·건물 색상과 지역 설명을 적용했습니다.
- 폐공장 기계·컨테이너, 주택가 마당·울타리, 병원 의료 표식·병상·구급차, 창고 상자·지게차, 군사시설 모래주머니·헬리패드, 숲 캠프 텐트·모닥불·통나무를 추가했습니다.
- 숲 캠프 중심으로 부쉬를 배치하고 다른 지역에도 소량 배치했습니다.
- 부쉬는 이동을 막지 않으며, 먼 거리에서는 안에 숨은 상대가 보이지 않습니다.
- 부쉬 안에서 사격하면 1.25초, 피격되면 0.8초 동안 위치가 노출됩니다.
- AI도 같은 은폐 규칙을 사용하며 총소리가 난 부쉬는 기존 소음 조사 로직으로 확인합니다.
- 현재 지역과 파밍 특성을 게임 화면에 표시합니다.

알려진 제한:

- 부쉬는 완전한 투사체 방어물이 아니며, 위치를 예상해 발사한 탄환은 적중할 수 있습니다.
- 장식물은 이번 버전에서 시각 요소이며 충돌 장애물로 사용하지 않습니다.
- 필드 채팅 고정 표시, 재장전 연출, 건물 끼임 추가 안정화는 Refactor 004C 범위입니다.


## Refactor 004C — 필드 UI, 재장전 연출, 충돌 안정화

- 경기 화면 왼쪽에 최근 필드 채팅 8개를 유지하고 근거리·관전·시스템 채널을 구분했습니다.
- 경기와 결과 화면 위에 복사 가능한 방 코드 배지를 항상 표시합니다.
- 서버 상태에 재장전 여부·무기·진행률을 동기화하고, 완료 전에는 탄창이 채워지지 않도록 했습니다.
- 캐릭터 위 원형 재장전 표시, 총기 슬롯 진행 막대, HUD 진행 문구를 추가했습니다.
- 빈 탄창으로 발사하면 예비 탄약이 있는 경우 자동 재장전을 시작합니다.
- 무기 교체·회복·사망 시 재장전 상태를 안전하게 취소합니다.
- 건물 문 최소 폭을 넓히고 서버 이동을 더 작은 단계로 나눴습니다.
- 플레이어와 AI 모두 벽과 겹친 경우 직전 안전 위치 또는 가장 가까운 유효 위치로 복구합니다.
- 클라이언트 예측 이동도 서버와 동일한 분할 이동 및 축별 슬라이딩을 사용합니다.

알려진 제한:

- 채팅은 현재 근거리 채널이며 전체 필드 전역 채팅 모드는 제공하지 않습니다.
- 샷건은 탄환 한 발씩 장전하는 방식이 아니라 탄창 단위 재장전입니다.
- 충돌은 경량 직사각형 벽 기반이며 전체 물리 엔진이나 NavMesh는 사용하지 않습니다.


## Refactor 004C UX Hotfix — 채팅 입력, 닉네임, 머리 위 말풍선

- Enter 입력을 DOM 채팅 input 한 곳에서 처리해 입력창이 전송 전에 닫히던 충돌을 수정했습니다.
- 채팅 input 이벤트가 Phaser 전역 키 처리로 전파되지 않도록 하여 Space 공백 입력과 한글 IME 조합을 정상화했습니다.
- 채팅을 여는 순간 서버에 중립 이동 입력을 보내 채팅 중 이동·공격·재장전 오작동을 차단했습니다.
- 서버가 playerId, 서버 권위 닉네임, 전송 시각을 포함한 메시지를 보낸 사람과 수신자에게 동일하게 전달합니다.
- 살아 있는 플레이어와 AI 위에 재사용 가능한 닉네임 오버레이를 표시하고 내 캐릭터에는 `(나)`를 붙입니다.
- 서버에서 수신한 근거리 채팅을 왼쪽 로그와 발신자 머리 위 말풍선에 동시에 표시합니다.
- 머리 위 말풍선은 4초 유지되고 마지막 1초 동안 사라지며 새 메시지로 교체됩니다.
- 부쉬로 보이지 않는 플레이어는 닉네임과 말풍선도 함께 숨깁니다.

알려진 제한:

- 음성 채팅, 팀 전용 채팅, AI 자동 채팅은 제공하지 않습니다.
- 말풍선은 최대 두 줄의 텍스트만 표시하며 커스텀 이모지는 지원하지 않습니다.

## Refactor 005 — 가시성, 미니맵, 수송기, 이동형 자기장, 필드 충돌

- 부쉬로 숨겨진 원격 플레이어도 위치 스냅샷 버퍼를 계속 갱신하고, 재등장 시 현재 보간 위치에서 짧게 페이드 인합니다.
- 캐릭터·닉네임·체력바·재장전 표시·말풍선·미니맵이 같은 부쉬 가시성 판정을 사용합니다.
- 미니맵에서 은폐된 적을 숨기고, 방 코드와 킬 로그를 미니맵 밖으로 재배치했습니다.
- F3 성능 패널에 FPS, RTT, 패치 간격, 추정 수신량, 서버 tick 평균/p95/max와 주요 처리 시간을 표시합니다.
- 네트워크 스냅샷은 플레이어·총알·아이템 객체를 ID 캐시로 갱신해 매 패치의 객체 재생성을 줄였습니다.
- 종이비행기 모양을 큰 탑다운 수송기 실루엣으로 교체하고, 8방향 랜덤 비행 경로를 서버에서 생성합니다.
- 다음 자기장 중심을 현재 원 내부에서 무작위로 정하고, 축소 중 중심과 반지름을 함께 보간합니다.
- AI는 급한 상황에서 현재/다음 안전 원의 서로 다른 분산 지점을 향해 이동합니다.
- 상자·울타리·병상·컨테이너·차량·기계·큰 나무·통나무·모래주머니·텐트를 실제 이동 및 총알 장애물로 사용합니다.
- 서버 이동, 클라이언트 예측, 넉백, 총알, 아이템 스폰과 AI 우회 경로가 같은 필드 충돌 데이터를 사용합니다.

알려진 제한:

- 필드 오브젝트는 파괴되지 않습니다.
- 차량 탑승과 낮은 엄폐물 높이 판정은 제공하지 않습니다.
- 회전된 통나무 등은 첫 버전에서 축 정렬 사각형 충돌로 판정합니다.
- F3 수신 데이터량은 브라우저 전송 계층의 정확한 바이트가 아니라 상태 개수 기반 추정치입니다.
- 실제 LAN 2인 + AI 6인 지연은 사용자 네트워크와 Windows 환경에서 추가 확인이 필요합니다.

## Refactor 005B — 건물 시야·로비·방 관리·자동 장착

- 서버 권위 `buildingId`와 공통 건물 가시성 구역 추가
- 건물 실내·실외 및 서로 다른 건물 플레이어 시야 분리
- 동일 출입문 근처에서만 제한적인 실내외 시야 허용
- 외부에서는 지붕으로 내부를 가리고, 내부에서는 현재 지붕을 페이드 아웃
- 실내 플레이어의 외부 영역 암전 마스크
- 캐릭터·닉네임·체력바·재장전·말풍선·미니맵 가시성 연동
- 건물 공간이 다른 총알과 바닥 아이템 렌더링 및 획득 안내 제한
- AI 벽 너머 탐지·근접 공격·총격 차단
- 로비 채팅 Enter·Space·한글 IME 입력 안정화
- 공개 방 목록과 코드 전용 방 지원
- 왼쪽 필드 채팅 로그 축소·확장 및 읽지 않은 메시지 표시
- 방장 표시와 서버 검증 강퇴 기능
- 로비·비행기·경기·관전·결과 화면의 방 나가기
- 방장 퇴장 시 가장 먼저 참가한 실제 사용자에게 자동 승계
- 무기 획득 후 서버 권위 즉시 장착 및 HUD 피드백

알려진 제한:
- 창문 시야와 창문 사격은 아직 지원하지 않음
- 문 열기·닫기와 잠금 기능은 아직 지원하지 않음
- 건물 2층과 계단은 아직 지원하지 않음
- 방 목록은 현재 서버 프로세스 안의 공개 방만 표시
- 실내 소리 감쇠와 팀 시야 공유는 아직 지원하지 않음

## Refactor 006 — 수송기·복구·전투 판정·저격소총·맵 선택

### 006H
- 수송기 본체·엔진·항법등을 건물 지붕보다 높은 공통 렌더 depth로 분리했습니다.
- 낙하 종료 시 건물 지붕·벽·solid 오브젝트·다른 플레이어와 겹치는 위치를 서버가 거부합니다.
- 잘못된 착지점은 가장 가까운 출입문 바깥 또는 주변 안전 지면으로 조정합니다.
- 플레이어와 AI의 최근 안전 위치를 저장하고 건물·오브젝트 끼임을 자동 복구합니다.
- 복구 좌표는 서버에서 확정하며 클라이언트 예측 위치도 함께 초기화합니다.

### 006A
- 이동·피격·플레이어 분리용 히트 반경을 shared 상수로 통합했습니다.
- 살아 있는 착지 플레이어가 완전히 겹치지 않도록 서버 권위 소프트 분리를 적용했습니다.
- 총알을 점 충돌이 아닌 이전 좌표에서 현재 좌표까지의 선분 충돌로 판정합니다.
- 여러 플레이어가 겹친 경우 발사 지점에서 가장 먼저 충돌한 유효 대상만 피격됩니다.
- 총알 생성 위치를 캐릭터 중심이 아닌 총구 앞쪽으로 이동했습니다.
- F3 모드에서 히트박스와 총알 이동 선분을 확인할 수 있습니다.

### 006B
- 탄약을 `샷건탄`, `일반 총알`, `권총탄` 세 종류로 통합했습니다.
- 기관단총·돌격소총·일반 소총·저격소총은 모두 일반 총알을 사용합니다.
- 기존 소총탄·기관단총탄·저격탄 ID를 일반 총알로 변환하는 호환 함수를 추가했습니다.
- 무기별 탄속·사거리·수명·피해·넉백을 공통 ProjectileConfig로 관리합니다.
- 총알은 피격·장애물 충돌·사거리·수명·맵 경계·경기 종료 조건에 따라 제거됩니다.
- 서버 제거 큐와 클라이언트 ID 캐시 정리로 총알 객체 누적을 방지합니다.

### 006C
- 저격소총과 4발 탄창을 추가하고 일반 총알을 사용하도록 했습니다.
- 저격소총 장착 시 카메라가 부드럽게 넓어지고 우클릭 조준 시 추가 확대와 조준 방향 이동을 적용합니다.
- 저격탄 속도를 일반 소총보다 크게 높이고 선분 충돌로 장거리 터널링을 방지합니다.
- 저격소총은 느린 연사·긴 재장전·발사 위치 노출·비관통 규칙을 유지합니다.

### 006D
- 기존 맵을 `작은 맵`, 확장 맵을 `큰 맵`으로 등록했습니다.
- 방장만 대기실에서 맵을 선택할 수 있으며 게임 시작 후에는 변경할 수 없습니다.
- 방 목록에 선택된 맵을 표시하고 전체·작은 맵·큰 맵 필터를 추가했습니다.
- 리매치는 현재 맵을 유지하고 맵 변경 시 정적 월드·미니맵·보간·카메라 캐시를 다시 생성합니다.

### 006E
- 큰 맵의 크기를 6144×6144로 확장하고 기존 지역·건물과 추가 대형 건물을 배치했습니다.
- 대형 건물에 출입구·내부 칸막이·넓은 파밍 공간을 적용했습니다.
- 맵별 비행 경로·자기장 반지름과 시간·파밍 예산·미니맵 스케일을 분리했습니다.
- 큰 맵의 군사·공장·물류 지역에서 저격소총과 일반 총알 등장 가능성을 높였습니다.

알려진 제한:
- 옥상 플레이, 건물 2층과 계단은 지원하지 않습니다.
- 저격 헤드샷, 탄도 낙차, 바람, 벽·플레이어 관통탄은 지원하지 않습니다.
- 큰 건물 내부 경로는 경량 가시성 그래프 기반이며 완전한 NavMesh는 아닙니다.
- 차량 운전과 파괴 가능한 건물은 지원하지 않습니다.
- 실제 Windows LAN의 인간 2명 + AI 6명 성능은 사용자 환경에서 추가 확인이 필요합니다.

## Refactor 007

### 007A · Sniper Scope Input
- 저격소총 우클릭 자동발사 제거
- 좌클릭 발사와 우클릭 스코프 입력 분리
- 채팅·재장전·회복·무기 교체·사망 시 스코프 자동 해제

### 007B · Scope Movement & Camera
- 스코프 상태 서버 이동속도 60%
- 부드러운 줌 및 조준 방향 카메라 리드
- 스코프 조준선·암전·안내 UI

### 007C · Motorcycle Core
- 작은 맵 3대, 큰 맵 6대 오토바이 배치
- E 탑승·안전 하차
- 서버 권위 가속·브레이크·회전·월드 충돌
- 원격 오토바이 위치·회전 보간

### 007D · Vehicle Shooting Accuracy
- 오토바이 이동 중 좌클릭 사격
- 속도와 회전량 기반 탄 퍼짐 증가
- 권총·일반 총기·샷건·저격소총별 차량 사격 페널티
- 최고속도 15% 초과 시 저격 스코프 제한

### 007E · Motorcycle Collision Damage
- 속도 기반 사람·AI 충돌 피해
- 진행 방향 넉백과 차량 속도 감소
- 대상별 충돌 쿨타임으로 반복 피해 방지
- 차량 끼임 및 비정상 위치 자동 복구
- 오토바이 충돌 전용 킬 원인

### 알려진 제한
- AI 오토바이 운전 미구현
- 2인 탑승 및 뒷좌석 사격 미구현
- 차량 연료·수리·폭발 미구현
- 오토바이 점프·윌리·드리프트 미구현
- 고속 저격 스코프 미지원

## Refactor 007 UX Hotfix — 오토바이 직접 이동·무기 교환 드롭

### 오토바이
- 오토바이를 캐릭터와 동일한 화면 기준 WASD 직접 이동 방식으로 변경했습니다.
- 대각선 입력을 정규화해 직선 이동보다 빨라지지 않도록 했습니다.
- 오토바이 외형은 실제 이동 velocity 방향으로 부드럽게 회전하며 정지 시 마지막 방향을 유지합니다.
- 이동 방향과 마우스 총기 조준 방향을 분리했습니다.
- 이동 사격 퍼짐은 실제 이동속도와 이동 방향 변화량을 기준으로 계산합니다.
- 충돌 피해와 넉백도 실제 이동 velocity 방향을 기준으로 처리합니다.
- X축과 Y축 충돌을 따로 해결해 벽 모서리에서 가능한 축으로 미끄러지며 이동합니다.

### 무기 교환
- 빈 총기 슬롯이 있으면 새 무기를 빈 슬롯에 저장합니다.
- 총기 슬롯이 모두 차 있으면 현재 장착한 총을 바닥에 떨어뜨리고 새 총을 같은 슬롯에 장착합니다.
- 떨어진 총은 기존 탄창 수, 건물 공간과 `buildingId`를 유지합니다.
- 플레이어의 예비 탄약은 그대로 유지합니다.
- 방금 버린 본인에게만 0.85초 재획득 잠금을 적용하며 다른 플레이어는 즉시 획득할 수 있습니다.
- 서버가 교체 슬롯과 안전한 드롭 위치를 먼저 검증한 뒤 한 번에 상태를 반영해 총기 복제와 증발을 방지합니다.
- AI도 동일한 무기 교환 함수를 사용하고 방금 버린 총을 즉시 다시 줍지 않습니다.

알려진 제한:
- 차량식 조작과 캐릭터형 조작을 선택하는 옵션은 없습니다.
- 수동 총기 버리기, 탄약 버리기, 인벤토리 드래그 앤 드롭은 제공하지 않습니다.
- AI 오토바이 운전은 지원하지 않습니다.


## Refactor 004C UX Hotfix — 채팅 입력, 닉네임, 머리 위 말풍선

- Enter 입력을 DOM 채팅 input 한 곳에서 처리해 입력창이 전송 전에 닫히던 충돌을 수정했습니다.
- 채팅 input 이벤트가 Phaser 전역 키 처리로 전파되지 않도록 하여 Space 공백 입력과 한글 IME 조합을 정상화했습니다.
- 채팅을 여는 순간 서버에 중립 이동 입력을 보내 채팅 중 이동·공격·재장전 오작동을 차단했습니다.
- 서버가 playerId, 서버 권위 닉네임, 전송 시각을 포함한 메시지를 보낸 사람과 수신자에게 동일하게 전달합니다.
- 살아 있는 플레이어와 AI 위에 재사용 가능한 닉네임 오버레이를 표시하고 내 캐릭터에는 `(나)`를 붙입니다.
- 서버에서 수신한 근거리 채팅을 왼쪽 로그와 발신자 머리 위 말풍선에 동시에 표시합니다.
- 머리 위 말풍선은 4초 유지되고 마지막 1초 동안 사라지며 새 메시지로 교체됩니다.
- 부쉬로 보이지 않는 플레이어는 닉네임과 말풍선도 함께 숨깁니다.

알려진 제한:

- 음성 채팅, 팀 전용 채팅, AI 자동 채팅은 제공하지 않습니다.
- 말풍선은 최대 두 줄의 텍스트만 표시하며 커스텀 이모지는 지원하지 않습니다.

## Refactor 008 Combat & Vehicle Balance Hotfix

### 전 총기 마우스 에임
- 화면 마우스 좌표를 현재 Phaser 카메라의 월드 좌표로 변환하고 총구 기준 정규화 조준 벡터를 생성합니다.
- 클라이언트는 `aimX`, `aimY`를 전송하고 서버가 유한값·최소 길이를 검사한 뒤 다시 정규화해 최종 발사 각도를 결정합니다.
- 권총·기관단총·돌격소총·샷건·저격소총·오토바이 이동 사격이 같은 조준 기준을 사용합니다.
- 일반 플레이의 분홍 조준점과 긴 조준선을 제거하고 F3 성능 패널을 켰을 때만 실제 조준 방향 디버그 선을 표시합니다.
- 무기 종류, 이동, 연사 반동과 오토바이 속도에 따라 마우스 위치 십자선이 확장됩니다.

### 방향형 저격 스코프
- 화면 중앙 고정 스코프를 제거하고 마우스 위치를 실제 스코프 십자선과 렌즈 중심으로 사용합니다.
- 내 캐릭터에서 마우스 방향으로 이어지는 전방 시야 통로와 타원형 렌즈를 제외한 화면을 강하게 암전합니다.
- 스코프 줌을 `0.82`로 조정하고 조준 거리의 일부만 최대 220px까지 부드럽게 선행합니다.
- 기존 건물 공간, 내부 벽, 부쉬 은폐 판정을 먼저 적용한 뒤 스코프 전방 시야와 거리 조건을 추가합니다.
- 캐릭터가 보이지 않으면 닉네임·체력바·말풍선도 함께 숨기고, 스코프 중 미니맵·방 정보·채팅 등 비전투 HUD를 감춥니다.
- 무기 교체, 재장전, 회복, 채팅, 사망, 관전, 탑승·하차와 오토바이 이동 시 스코프를 해제합니다.

### 오토바이 밸런스
- 출발속도를 플레이어의 1.05배, 최고속도를 약 1.85배로 낮췄습니다.
- 같은 방향 입력을 유지하면 약 2.4초 동안 `1.05 → 1.15 → 1.50 → 1.85배`로 단계 가속합니다.
- 90도 이상 급회전과 반대 방향 전환, 벽 충돌, 사람 충돌, 끼임 복구 시 속도와 가속 누적을 줄입니다.
- 입력 해제 후 약 0.48초 안에 정지하고 탑승 직후 이전 운전자의 속도와 가속 상태를 이어받지 않습니다.
- 최고속도의 40% 미만에서는 충돌 피해가 없으며 최대 충돌 피해를 45로 제한했습니다.
- 탑승 후 0.5초 동안 차량 충돌 피해를 막고 사람을 들이받은 차량도 큰 폭으로 감속합니다.
- 실제 속도 구간과 급회전에 따라 이동 사격 퍼짐을 늘리고 거의 정지한 상태에서만 저격 스코프를 허용합니다.

알려진 제한:
- 렌즈 경계 블러, 광학 왜곡, 배율 단계와 숨 참기 기능은 포함하지 않습니다.
- AI 오토바이 운전, 차량 연료·체력·폭발, 2인 탑승은 지원하지 않습니다.
- 실제 Windows LAN 인간 2명 + AI 6명 체감 검증은 사용자 환경에서 별도로 수행해야 합니다.

## Refactor 008A — LAN 시작 주소 Hotfix

- 일반 개발 및 프로덕션 시작에서도 같은 와이파이용 IPv4 접속 주소를 출력합니다.
- 가상·링크 로컬 주소보다 10.x, 172.16~31.x, 192.168.x 사설 IPv4를 우선합니다.
- 빌드 서버용 `pnpm start:lan` 명령을 추가합니다.
- 접속 확인용 `/api/health` 주소를 함께 출력합니다.

## Refactor 009 Window Transit Hotfix

### 창문 생성
- 건물 크기와 건물 인덱스를 기준으로 작은 건물 1개, 중간 건물 2개, 큰 건물 최대 3개의 창문을 결정적으로 생성합니다.
- 창문은 문과 모서리를 피하고 서버와 클라이언트가 shared의 같은 좌표를 사용합니다.
- 작은 맵과 큰 맵 모두 창문 ID, 개구부, 실내외 넘기 지점을 동일하게 계산합니다.

### 창문 인·아웃
- 착지 상태에서 Space 키를 누르면 서버가 가장 가까운 창문과 반대편 안전 착지점을 검증합니다.
- 약 320ms 동안 창문을 넘고 진행률 50%에서 `buildingId`를 한 번만 전환합니다.
- 넘기 중 이동·발사·치료·재장전·무기 교체·탑승·아이템 획득을 막고 피해는 정상적으로 받습니다.
- 반대편이 벽, 오토바이 또는 다른 플레이어로 막히면 요청을 거부하며 완료 시 안전 위치를 다시 검증합니다.

### 충돌과 사격
- 이동 장애물은 기존 건물 벽을 유지해 걷기, 넉백, AI 이동과 오토바이를 차단합니다.
- 총알 및 시야 장애물은 창문 개구부만 제외해 창문 중앙 사격은 통과하고 창문 옆 벽은 계속 차단합니다.
- 권총, 기관단총, 돌격소총, 샷건 산탄, 저격탄이 같은 서버 선분 충돌 규칙을 사용합니다.

### 가시성과 상호작용
- 실내외 대상의 연결 선분이 같은 창문 개구부를 통과하고 양쪽 가시 거리 안에 있을 때만 보입니다.
- 기존 부쉬 은폐, 내부 벽, 방향형 저격 스코프 판정을 그대로 유지합니다.
- 캐릭터의 최종 가시성 결과를 닉네임, 체력바, 말풍선과 미니맵 표시가 함께 사용합니다.
- 창문 너머 총기, 탄약, 치료 아이템과 오토바이는 보이더라도 직접 획득하거나 탑승할 수 없습니다.

알려진 제한:
- 창문 유리 파괴, 유리 파편, 창문 개폐와 AI 창문 넘기는 포함하지 않습니다.
- 창문은 이미 열려 있거나 깨져 있는 개구부로 표현합니다.
- 실제 PC 두 대 LAN 동기화와 창문 연출 체감은 사용자 환경에서 별도로 확인해야 합니다.

## Refactor 010 Window Combat & Vehicle Destruction Hotfix

### 비차단 게임 경고
- 브라우저 기본 `alert`, `confirm`, `prompt` 사용을 제거했습니다.
- 회복 아이템 부족, 재장전 탄약 부족, 창문 착지 공간 부족과 차량 파괴 경고를 게임 화면의 비차단 알림으로 표시합니다.
- 알림은 입력 포커스를 가져가지 않고 자동으로 사라지며, 같은 메시지는 0.9초 동안 중복 표시하지 않고 최대 3개까지만 유지합니다.
- 방 강퇴와 진행 중 방 나가기는 팝업 대신 2.2초 안에 버튼을 한 번 더 누르는 방식으로 확인합니다.

### 탑승 차량 가시성
- 탑승 중인 오토바이는 운전자의 최종 가시성을 그대로 사용합니다.
- 운전자가 건물 벽, 부쉬 또는 저격 스코프 시야 밖에 숨으면 오토바이와 차량 내구도 표시도 함께 숨습니다.
- 서버가 오토바이 탑승을 이유로 운전자의 부쉬 은폐를 강제로 해제하던 처리를 제거했습니다.
- 빈 오토바이는 플레이어 부쉬 은폐가 아니라 월드 벽과 스코프 시야 판정을 적용합니다.

### 창문 전투
- 창문 시야의 기존 실내 180px·실외 210px 고정 제한을 제거했습니다.
- 대상 몸 중심과 좌우 표본점에 대한 선분 가시성을 검사해 창문에 일부 노출된 플레이어를 표시합니다.
- 표본점 하나 이상이 열리면 캐릭터를 표시하고, 몸 중심 또는 두 개 이상이 열릴 때 닉네임·체력바·말풍선을 표시합니다.
- 클라이언트 가시성, 서버 AI 시야, 총알 충돌이 같은 창문·문 개구부와 solid 벽 데이터를 사용합니다.
- 건물 ID가 달라도 실제 총알 선분이 창문이나 문을 통과하면 실내외 양방향 피격이 가능합니다.
- 창문 너머 아이템 획득과 오토바이 탑승 차단은 그대로 유지합니다.

### 오토바이 내구도와 폭발
- 오토바이에 최대 내구도 180과 서버 권위 파괴 상태를 추가했습니다.
- 권총·기관단총·돌격소총·샷건·저격소총이 무기별 차량 피해 배율로 오토바이를 공격합니다.
- 샷건은 한 번의 사격으로 차량에 줄 수 있는 피해를 최대 30으로 제한합니다.
- 고속 벽 충돌과 플레이어 충돌도 차량 내구도를 감소시키며, 동일 벽 반복 충돌 피해에는 350ms 쿨타임을 적용합니다.
- 내구도 30% 이하에서 연기와 위험 경고를 표시합니다.
- 내구도 0에서 운전자를 안전 위치로 강제 하차시키고 700ms 후 서버 권위 폭발을 발생시킵니다.
- 폭발은 중심 최대 55, 외곽 최소 8의 거리 감쇠 피해와 넉백을 적용합니다.
- solid 벽은 폭발을 차단하고 문과 창문 개구부를 향한 유효한 선분은 피해를 전달합니다.
- 다른 오토바이는 폭발 피해로 연쇄 파괴될 수 있으며 각 차량은 자체 700ms 퓨즈를 사용합니다.
- 최근 5초 이내 차량을 공격한 플레이어가 있으면 차량 폭발 처치를 해당 공격자에게 귀속합니다.

알려진 제한:
- 실제 PC 두 대에서의 차량 은폐, 창문 장거리 저격과 폭발 동기화는 사용자 LAN 환경에서 수동 확인이 필요합니다.
- 영구 차량 잔해, 차량 수리·연료, 지속 화염 피해와 건물 파괴는 포함하지 않습니다.
- 박격포와 연막탄은 후속 패치로 분리합니다.

## Refactor 010A Window Visibility & Death Presentation Emergency Hotfix

### 자연스러운 창문 국소 시야
- 실내에서 창문 바깥이 화면 끝까지 직사각형 띠로 열리던 마스크를 제거했습니다.
- 창문 개구부에서 시작해 멀어질수록 완만하게 넓어지는 사다리꼴 국소 시야 볼륨을 사용합니다.
- 일반 상태 창문 시야 깊이는 300px, 저격 스코프 상태는 480px로 제한합니다.
- 창문 가장자리에 38px 완화 구간을 두어 공개 영역이 딱 잘린 사각형처럼 보이지 않게 했습니다.
- 외부 관찰자가 창문이나 문을 통해 실내 대상을 볼 때 건물 지붕 전체를 투명하게 만들지 않고 해당 개구부 주변만 국소적으로 공개합니다.
- 대상 몸 중심과 좌우 표본점이 실제 개구부와 정렬되고 국소 시야 볼륨 안에 있을 때만 캐릭터를 표시합니다.
- 한 표본점만 노출되면 캐릭터만 표시하고, 몸 중심 또는 두 표본점 이상이 노출될 때만 닉네임·체력바·말풍선을 표시합니다.
- 창문 옆 벽과 실내 깊숙한 대상은 기존 solid 벽, 부쉬 및 방향형 스코프 판정에 따라 계속 숨깁니다.
- 창문 사격, 창문 넘기와 창문 너머 아이템 상호작용 차단은 기존 서버 권위 규칙을 유지합니다.

### 캐릭터 사망 연출
- 서버는 체력 0에서 기존처럼 즉시 사망·킬·드롭·승리 판정을 처리하고, 클라이언트만 마지막 위치에 짧은 사망 잔상을 표시합니다.
- 사망 이벤트에 위치, 공격 원인과 마지막 공격 방향을 포함해 일반 총기, 저격, 샷건, 차량 충돌, 차량 폭발과 자기장에 약간 다른 밀림을 적용합니다.
- 사망 잔상은 짧은 피격 플래시, 밀림, 납작해지는 쓰러짐과 약 0.9~1.2초 페이드 후 제거됩니다.
- 잔상에는 충돌, 피격, 상호작용, 닉네임, 체력바와 말풍선이 없으며 Loot 생성과 위치를 변경하지 않습니다.
- 로컬 플레이어는 사망 직후 약 700ms 동안 마지막 위치에 카메라가 머문 뒤 기존 관전 또는 결과 흐름으로 전환합니다.
- 사망 직전에 보이지 않았던 벽·부쉬·창문 시야 밖 원격 대상의 사망 연출은 새로 표시하지 않아 위치 노출을 방지합니다.
- 오토바이 탑승 중 운전자가 사망하면 차량 연결만 해제하고, 차량 내구도와 폭발 상태는 기존 규칙을 그대로 유지합니다.

알려진 제한:
- 부드러운 마스크의 실제 체감, 창문 방향별 시야 폭과 사망 Tween 자연스러움은 사용자 LAN 브라우저 환경에서 수동 확인이 필요합니다.
- 래그돌, 고어, 영구 시체, 킬캠, 다운드 상태와 오토바이 피격 판정 개편은 포함하지 않습니다.

## Refactor 010B Window Proximity & Motorcycle Explosion Lethality Hotfix

### 창문 근접 시야
- 실내에서는 일반 110px·저격 135px, 실외에서는 일반 95px·저격 125px 이내로 창문에 접근해야 시야가 활성화됩니다.
- 활성 거리 안에서 마우스 조준 방향과 가장 잘 맞는 창문 하나만 선택하며, 22도 신규 선택·28도 유지 각도로 창문 전환 깜빡임을 줄였습니다.
- 실내에서 외부는 일반 170px·저격 290px, 외부에서 실내는 일반 105px·저격 165px까지만 국소적으로 공개합니다.
- 일반 시야 확장률을 0.08, 저격 시야 확장률을 0.05로 낮춰 넓은 탐조등처럼 퍼지는 현상을 완화했습니다.
- 선택되지 않은 창문은 대상 캐릭터·차량·총알·사망 잔상을 새로 노출하지 않습니다.
- 문 이동, 창문 넘기, 창문 중앙 사격과 창문 너머 아이템 상호작용 차단은 기존 규칙을 유지합니다.

### 오토바이 폭발 치명도
- 폭발 퓨즈를 850ms, 반경을 160px로 조정했습니다.
- 폭발 중심 최대 피해를 90으로 올려 풀피 플레이어가 중심부에서 맞으면 약 10 HP가 남습니다.
- 피해는 50px·90px·125px·160px 구간을 기준으로 90→85→68→38→12까지 부드럽게 감소합니다.
- 직접 노출과 열린 문은 피해 100%, 창문 개구부는 피해 70%, solid 벽 뒤는 피해 0%를 적용합니다.
- 강화된 폭발 피해와 넉백, 연쇄 폭발, 최근 공격자 킬 귀속과 010A 사망 잔상 연출을 유지합니다.
- 파괴 대기 차량은 흐린 위험 반경을 표시하고 `차량 파괴 · 폭발 위험` 비차단 경고를 출력합니다.

알려진 제한:
- 창문 시야 마스크의 최종 밝기와 창문 선택 감각은 실제 해상도와 LAN 환경에서 수동 조정이 필요할 수 있습니다.
- 오토바이 탑승자와 차체의 별도 피격 영역 개편은 이번 긴급 패치에 포함하지 않습니다.

## Refactor 010C Door Portal Visibility Emergency Hotfix

### 문 시야
- 문이 항상 외부를 탐조등처럼 공개하던 처리를 제거했습니다.
- 실내에서는 문 중심 145px(스코프 175px), 실외에서는 120px(스코프 150px) 이내에서 문을 실제로 조준할 때만 시야가 활성화됩니다.
- 문 시야 깊이를 실내→실외 220px(스코프 330px), 실외→실내 145px(스코프 210px)로 제한했습니다.
- 문 시야 확장률을 일반 0.10, 스코프 0.06으로 제한하고 끝부분과 가장자리의 부분 공개를 완화했습니다.
- 문 통과 시 선택 포털을 최대 300ms 유지해 실내외 전환 순간의 깜빡임을 줄였습니다.

### 통합 포털
- 문과 창문을 하나의 포털 선택 대상으로 통합하고 동시에 하나만 활성화합니다.
- 신규 포털은 조준 각도 24도 이내, 기존 포털은 30도 이내에서 유지하는 히스테리시스를 적용했습니다.
- 선택되지 않은 문과 창문은 이동과 총알 통과 기능을 유지하지만 캐릭터·닉네임·사망 잔상 시야를 열지 않습니다.
- 다른 건물 내부로 포털 시야가 연속 연결되지 않도록 기존 건물 공간 차단 규칙을 유지합니다.
- 010B의 창문 근접 시야와 오토바이 폭발 수치는 변경하지 않았습니다.

## Refactor 010D Angle-Aware Portal Visibility Hotfix

### 위치 기반 포털 시야
- 문과 창문의 고정 삼각형 시야를 관찰자 위치에 따라 기울어지는 비대칭 절두체로 교체했습니다.
- 포털 개구부의 좌우 끝점과 관찰자의 실제 좌표를 사용하므로 왼쪽·중앙·오른쪽 이동에 따라 시야가 연속적으로 변합니다.
- 근접 활성 판정은 포털 개구부 주변 캡슐 거리로 계산하고 실제 가시 영역은 개구부를 통과하는 광선으로 분리했습니다.

### 정면·측면·주변 시야
- 조준 각도 0~20도는 정면, 20~40도는 측면, 40~60도는 주변 시야로 부드럽게 감쇠합니다.
- 측면에서는 시야 깊이와 밝기가 감소하고, 주변 시야에서는 캐릭터만 제한적으로 표시하며 닉네임·체력바·말풍선·미니맵 신규 표시는 숨깁니다.
- 60도를 초과하면 포털 시야를 완전히 닫습니다.

### 근접 시야 폭발 방지
- 창문은 최소 55px, 문은 최소 65px 뒤의 가상 렌더 시점을 사용해 포털 바로 앞에서 화면 전체가 열리는 현상을 방지했습니다.
- 최대 전체 시야각을 창문 30도, 문 38도로 제한했습니다.
- 실제 대상 가시성 선분은 가상 시점이 아니라 플레이어의 실제 위치를 계속 사용합니다.

### 회귀 유지
- 문·창문 통합 활성 포털 1개, 근접 거리, 선택 히스테리시스, 문 통과 300ms 유지 규칙을 보존했습니다.
- 문·창문 총알 통과, 창문 넘기, 아이템 상호작용 차단, 부쉬 은폐, 저격 스코프, 사망 연출, 오토바이 폭발 수치는 변경하지 않았습니다.


## Refactor 011 Sound Foundation & Combat Audio Pass

- Web Audio API 기반 단일 AudioManager와 외부 음원 없는 합성 효과음을 추가했습니다.
- 권총·기관단총·돌격소총·샷건·저격소총 발사음, 재장전, 빈 탄창, 명중·킬·피격 사운드를 구분했습니다.
- 실제 이동 거리 기반 발소리, 부쉬 이동음, 창문 넘기, 아이템·회복 피드백을 연결했습니다.
- 오토바이 속도 기반 엔진 루프, 충돌·피격·치명 상태·850ms 폭발 경고·폭발음을 구현했습니다.
- 좌우 패닝, 거리 감쇠, solid 벽·문·창문 차폐와 저역 통과 필터를 적용했습니다.
- 전체·효과음·환경음·음악·음소거 설정을 `drop8_audio_settings_v1`에 저장합니다.
- 브라우저 사용자 입력으로 AudioContext를 활성화하며 탭당 하나의 컨텍스트만 사용합니다.

## Refactor 012 Throwable Arsenal

### 공통 투척 시스템
- `DROP8_REFACTOR_012_THROWABLE_ARSENAL` 공통 모듈을 추가했습니다.
- `4`는 단일 투척무기 슬롯, `Q`는 기존 자동 회복으로 변경했습니다.
- 좌클릭을 누르는 동안 최대 0.85초 충전하며 120~480px 범위로 투척합니다. 우클릭 또는 1~3 전환으로 준비를 취소합니다.
- 클라이언트 가이드와 서버 실제 이동이 shared의 동일한 x/y/z·중력·튕김 계산을 사용합니다.
- 점선 포물선, 첫 충돌, 한 차례 튕김, 최종 착지점, 효과 반경과 충전 막대를 본인에게만 표시합니다.
- 열린 문과 올바른 높이의 창문 개구부를 통과하며, 벽·창틀·명시적으로 닫힌 문은 튕김 충돌로 처리합니다.
- 투척물·연막·화염 네트워크 상태 증가에 대비해 Colyseus schema 인코더 버퍼를 64KiB로 확장했습니다.

### 투척무기와 피해
- 파편 수류탄: 2.5초 신관, 180px 반경, 플레이어 100/75/45/20, 차량 120/80/45/20 거리 구간 피해입니다.
- 연막탄: 피해 0, 부쉬 대표 반지름의 2배, 0.7초 전개 지연 후 1.2초 확장·8초 유지·1.5초 소멸입니다.
- 같은 연막 안에서는 80px 이내 캐릭터 실루엣만 흐리게 보이며 닉네임·체력바·말풍선은 숨깁니다.
- 화염탄: 충돌 즉시 115px 화염을 만들고 초기 8, 0.25초마다 3, 8초 지속 피해를 줍니다. 중첩 화염은 한 영역만 적용합니다.
- 화염 차량 피해는 초기 8, 초당 8입니다. 화염에서 벗어나면 잔여 화상은 없습니다.
- 폭발과 화염 피해, 소지 수량, 처치 귀속은 서버가 확정합니다.

### 인벤토리·AI·오디오
- 파편탄·연막탄·화염탄 중 한 종류만 최대 3개까지 소지합니다.
- 같은 종류는 합쳐지고 다른 종류 획득 시 기존 스택을 바닥에 떨어뜨린 뒤 교체합니다.
- AI는 직접 투척하지 않으며 연막 너머를 보지 못하고, 가까운 파편탄과 화염 지역에서 이탈을 시도합니다.
- Refactor 011 AudioManager에 선택·준비·투척·튕김·폭발·연막 전개·화염 점화·획득·교체 합성음을 추가했습니다.

### 검증과 알려진 제한
- shared 투척 규칙 테스트 9개와 서버 권한 통합 테스트 4개를 추가했습니다.
- 현재 기본 맵의 문은 항상 열린 포털입니다. `open:false` 문 데이터가 제공되면 투척 충돌은 닫힌 문으로 처리합니다.
- 수류탄 요리하기, 총알에 의한 폭발, 연쇄 폭발, 화염 확산, 부쉬 소실, AI 직접 투척은 포함하지 않습니다.

## Refactor 013 — Interior Rooms, Central River & Dock 8 Expansion

- `MapId`를 `small | large | dock8`로 확장하고 정적 맵 데이터를 shared 모듈로 분리했습니다.
- 플레이어와 전리품에 숫자형 `roomIndex`를 추가해 문자열 방 ID의 네트워크 중복을 피했습니다.
- 내부 문과 창문을 `SpacePortal`로 일반화하고 방별 시야, 내부 창문 vault, AI 방 이동을 추가했습니다.
- 기존 작은 맵 중앙 건물 24~26과 큰 맵 중앙·대형 건물에 명시적 전리품 앵커를 추가했습니다.
- 신규 7168×7168 `8번 부두` 맵, 굽은 중앙 강, 다리 3개, 여울 2개, 상륙점 22개, 전리품 172개, 오토바이 4대를 추가했습니다.
- 깊은 물 수영과 서버 권위 행동 제한을 추가했습니다. 수영 중 공격·재장전·투척·회복·습득·상호작용은 차단됩니다.
- 파편탄은 물에서 폭발하고 연막탄과 화염탄은 깊은 물에서 각각 수증기·소화 처리됩니다.
- AI는 방 포털과 강 횡단 후보를 비교하고 다리·여울·수영 경로를 선택하며 수영 중 공격하지 않습니다.
- 정적 강·방·포털·AI 그래프는 Colyseus 상태로 반복 전송하지 않습니다.


## Refactor 013G — Dock 8 Loot, Visibility, Interior Reveal, Shoreline & Mobility Recovery

- 전체 화면을 덮던 실내 시야 캔버스를 제거하고 현재 건물의 보이지 않는 다른 방만 어둡게 처리합니다. 현재 방과 로컬 플레이어는 즉시 완전히 표시됩니다.
- 기존 장애물·연막·수풀 시야를 유지하면서 방과 포털 시야를 추가 필터로 적용합니다.
- Dock 8 전리품 후보를 264개로 확장하고 경기당 216개를 생성합니다. 이 중 124개가 실내이며 모든 진입 가능 건물에 최소 2개 이상을 보장합니다.
- 오토바이 후보를 12곳으로 늘리고 매 경기 8대를 강 양쪽과 중앙 접근로에 균형 배치합니다.
- 물가·얕은 물·깊은 물, 여울과 상륙 가능 구간의 월드·미니맵 표시를 강화합니다.

## Refactor 013H — Legacy Visibility & Roof Restoration, River Continuity, Delayed Zone & Long-Range Balance

- RoomZone을 독립 지붕으로 취급하지 않고 Building 단위 지붕과 방별 occlusion을 분리했습니다.
- 마지막 정상 실내·실외 시야의 관전자별 포털 cone/선분 판정을 roomIndex 구조에 이식했습니다.
- 공용 `terrainAt()`을 추가해 건물·다리·여울·shore·얕은 물·깊은 물 판정 우선순위를 통일했습니다.
- 초반 자기장 비활성 단계와 15초 첫 원 예고를 추가하고 첫 원 중심의 중앙 편향을 완화했습니다.
- 작은 맵 무기 비율은 유지하면서 큰 맵과 Dock 8의 저격총 및 관련 보급 가중치를 조정했습니다.

## Refactor 013H FIXED v3: 시야·수영·자기장·AI 안정화
- 유효한 AI 경로는 시간 경과만으로 폐기하지 않고, 목표 변경 또는 실제 고착 때만 재탐색합니다.
- 실내 지붕, 방 차폐, 문·창문 시야와 엔티티 표시를 분리합니다.
- 건물·방은 강보다 우선하는 육지이며 강 전체의 깊은 물에서 연속 수영합니다.
- 모든 일반 모드는 자유 파밍 후 첫 안전구역을 예고하며 첫 원을 맵 전역에 분산합니다.
- 큰 맵과 8번 부두의 장거리 전리품 비율을 맵별로 보정합니다.

## Refactor 013H1 — Large Map Nested Rooms & Bidirectional Windows, Dock 8 Terrain & Natural Loot Hotfix

### 큰 맵 중첩 공간과 지붕
- 겹치는 Building은 면적이 작은 가장 구체적인 공간을 우선하고, 동일 Building의 중첩 RoomZone도 가장 작은 유효 방을 현재 방으로 선택합니다.
- 로컬 관전자의 위치를 포함하는 모든 Building roof를 함께 reveal해 자식 건물 안에서 부모 지붕이 다시 덮이는 현상을 제거했습니다.
- 실내 overlay는 네트워크 roomIndex만 기다리지 않고 로컬 좌표의 공용 공간 판정을 우선하며, 전환 프레임에는 플레이어 반경도 투명 영역으로 보장합니다.

### 창문 양방향 vault
- 외벽 창문의 양쪽 접근점이 실제로 속한 Building·Room을 기록합니다. 겹친 큰 맵 건물에서는 창문 바깥쪽이 다른 Building 내부일 수 있습니다.
- 창문 후보는 빈 실외 buildingId에서도 근접 opening을 검색하며, 반대편의 정확한 공간 안에서 충돌 없는 착지점을 제한적으로 탐색합니다.
- 하나의 WindowPortal로 실내↔실외와 방 A↔방 B를 처리하고 외벽 충돌, 수영·차량 제한과 서버 권위 이동을 유지합니다.

### Dock 8 물과 전리품
- 서버는 굽은 RiverBand를 선분 캡슐의 합집합으로 판정하고 있었지만 클라이언트는 각 굵은 선분을 butt cap으로 그려 굽이 사이에 녹색 삼각형 틈을 남겼습니다. Dock 8 수면의 각 선분 끝에 동일 반지름 원을 그려 시각 경계를 서버 판정과 맞췄습니다.
- Dock 8 전리품 후보는 380개, 활성 예산은 280개로 확장했습니다. 활성 실내 앵커는 170개로 약 60.7%입니다.
- 그리드 앵커와 실제 스폰에 재현 가능한 시드 기반 jitter, RoomZone·지형·벽·문·창문·아이템 간격 검사를 적용했습니다.
- 작은 맵 전리품 85개, 큰 맵 전리품 142개, Dock 8 오토바이 8대와 FIXED v3의 저격총 가중치는 유지합니다.


## Refactor 015A — 보급 상자 및 화염방사기
- 두 번째 자기장 축소 시작 시 서버 권위 보급 상자 1회 투하
- 바주카포 45% / 화염방사기 55%, 전용 탄약 2묶음과 회복·일반탄 포함
- 화염방사기 탱크 100, 예비 연료 200, 0.1초당 연료 1·플레이어 3·오토바이 2 피해
- 오토바이 정지·주행 중 모두 사용 가능하며 벽·닫힌 공간 판정은 서버가 검증

## Refactor 017 — Adhesive Sprayer, Strip Trap, Lobby Recovery & Bazooka Water Fix

<!-- DROP8_REFACTOR_017_ADHESIVE_STRIP_LOBBY_BAZOOKA_WATER -->

### 점착 분사기
- 독립 무기 `adhesive_sprayer`와 충전량 `adhesive_charge`를 추가했습니다.
- 315px, 반각 21도의 서버 권위 원뿔 판정을 사용하며 직접 피해는 없습니다.
- 오토바이가 0.35초 이상 연속 노출되면 최고속도 0.55, 가속 0.40, 조향 0.75 배율이 기본 4초 적용됩니다. 연속 분사에 의한 종료 연장은 최초 발동 후 최대 6초로 제한됩니다.
- 점착 분사기 탄창 80, 최대 예비 충전량 160, 1틱당 1을 사용합니다.

### 로우프로파일 스트립 트랩
- 숫자 5로 전방 64px의 실외 단단한 지면에 설치합니다. 길이 100px, 폭 14px, 0.8초 활성화, 60초 수명, 30 HP입니다.
- 소유자당 최대 2개이며 세 번째 설치 시 가장 오래된 본인 트랩이 제거됩니다.
- 보행자는 발동시키지 않습니다. 오토바이는 최고속도 0.38, 가속 0.28, 조향 0.60 배율이 5.5초 적용되고 트랩은 즉시 제거됩니다.
- 총알은 벽·플레이어·차량과 동일한 선분 충돌 우선순위 안에서 트랩을 파괴합니다.

### 차량 상태 효과
- 점착과 트랩은 차량 원본 상수를 변경하지 않는 서버 내부 상태 관리자를 공유합니다.
- 활성 감속은 곱하지 않고 성능 항목별 가장 강한 값만 적용하며 최고속도 배율 하한은 0.30입니다.
- 차량 파괴, 효과 만료, 라운드 종료, 재경기와 방 정리 시 상태를 제거합니다.
- 현재 AI 오토바이 운전 기능은 새로 추가하지 않았으며, 효과는 운전자 종류가 아닌 차량 ID에 적용됩니다.

### 로비 공개 방 목록
- 프로세스 로컬 `Map` 대신 `matchMaker.query({ name: 'drop8' })`를 공개 방 목록의 원천으로 사용합니다.
- Room metadata에는 방 코드·방장·인원·단계·맵·공개 여부만 포함하며 비밀번호는 포함하지 않습니다.
- 기존 `/api/rooms`, 2.5초 폴링, 방 코드 직접 입장은 유지합니다.
- 클라이언트는 이전 요청을 취소하고 방 코드를 기준으로 중복을 제거하며 로딩·빈 목록·오류 상태를 구분합니다.

### 바주카포 수면 오폭
- 로켓 이동의 `terrainHit`에서 `deep-water`를 제거해 작은 맵 강, 큰 맵 강, Dock 8 수면을 통과할 수 있게 했습니다.
- 월드 경계, 최대 사거리, 수명, 벽·장애물·플레이어·오토바이 충돌 폭발은 유지합니다.

### 보존한 현재 수동 밸런스
- 화염방사기 사거리 420, 플레이어 틱 피해 9, 차량 틱 피해 6, 틱당 연료 1
- 보급 시작 단계 0, 바주카포 가중치 0, 화염방사기 가중치 100
- 바주카포 탄속 900, 차량 직접 피해 170, 최대/최소 폭발 피해 128/14, 구조물 피해 120
- 기존 총기 피해와 오토바이 기본 최고속도·가속·HP·충돌 피해

## Refactor 018 — DROP 8 Werewolf Season

<!-- DROP8_REFACTOR_018_WEREWOLF_SEASON -->

### 제단·저주·변신
- 작은 맵, 큰 맵, 8번 부두에 맵별 검증 후보를 추가하고 경기마다 제단과 은빛 무기함을 서로 충분히 떨어진 육지에 배치합니다.
- 제단은 15초 동안 휴면 상태로 월드에만 보이며, 이후 전체 알림·합성 울음 효과·미니맵 아이콘과 함께 활성화됩니다.
- 플레이어는 하차 후 제단 근처에서 `E`를 3초 유지합니다. 이동·피격·키 해제·범위 이탈·수영·vault·차량 탑승 시 의식이 취소됩니다.
- 의식 완료자는 전용 저주를 얻고 `F`로 1.2초 변신 준비를 시작합니다. 30초 안에 사용하지 않으면 안전 하차 후 강제 변신합니다.
- 변신은 30초 유지되며 HP·장비·탄창을 회복하거나 삭제하지 않습니다. 변신 중 총기·회복·투척·습득·장비 변경·차량 이용은 차단됩니다.
- 변신 전 저주 보유자가 사망하면 유효한 처치자에게 남은 시간이 이전되고, 이전할 수 없으면 안전 위치에 저주가 드롭됩니다.

### 늑대 이동·전투·오토바이 사냥
- 늑대 기본 속도는 현재 오토바이 최고속도의 110%, 우클릭 질주는 140%이며 실내에서는 410px/s 상한을 사용합니다.
- 질주는 최대 4초 게이지, 5초 완전 회복, 종료 후 0.6초 회복 지연을 사용하며 서버가 게이지와 제한 선회를 판정합니다.
- 좌클릭 할퀴기는 92px·반각 50도·34 피해·0.65초 쿨다운의 서버 권위 부채꼴 공격입니다.
- 늑대는 일반 총알 피해 35%, 폭발 피해 25%, 폭발 넉백 50%를 감소시키지만 추가 HP나 변신 회복은 없습니다.
- 오토바이 첫 할퀴기는 소량 피해와 2.5초 사냥 표식을 적용하고 최고속도 0.70·가속 0.75·조향 0.80 배율로 감속합니다.
- 같은 늑대가 표식 중 두 번째 할퀴기에 성공하면 기존 안전 하차 탐색을 사용해 운전자를 내리고 짧은 행동 잠금·공격 후딜·재발동 면역을 적용합니다.

### 은빛 무기함·은화살 쇠뇌
- 제단 활성화와 함께 모든 플레이어에게 위치가 공개되는 은빛 무기함을 활성화합니다.
- 무기함은 쇠뇌 1개와 은화살 5발을 보장합니다. 쇠뇌는 1발 장전, 1.8초 재장전, 1800px/s 탄속과 1100px 사거리를 사용합니다.
- 일반 플레이어 피해는 26이며 늑대에게는 방어구와 일반 저항을 우회하는 55 고정 피해를 적용합니다.
- 첫 은화살은 질주를 즉시 취소하고 2.5초 동안 기본 늑대 이동속도를 55%로 낮추며 감속 중 질주를 금지합니다.
- 쇠뇌는 오토바이 운전 중 발사·재장전할 수 있고 기존 차량 이동 사격 분산 규칙을 따릅니다.

### 반복 사이클·동기화·보존
- 늑대가 사망하거나 30초가 끝나면 제단은 10초 재충전 후 다시 활성화됩니다. 생존자 3명 이하, 최종 자기장, 경기 종료에서는 재활성화하지 않습니다.
- 재활성화 시 월드에 쇠뇌가 없으면 1개, 전체 은화살이 2발 미만이면 2발을 보충하며 무한 복제를 방지합니다.
- `PlayerState`와 `Drop8State`에는 각각 중첩 Schema 하나만 추가해 클래스당 64필드 한도를 지킵니다.
- AI의 제단 의식·변신·쇠뇌 사용·오토바이 운전은 추가하지 않았습니다. 기존 AI는 늑대를 공격하고 늑대에게 공격받을 수 있습니다.
- 오토바이 최고속도·가속·HP·충돌 피해, 바주카포 피해·재장전, 화염방사기와 기존 총기·전리품 밸런스는 변경하지 않습니다.


## Refactor 019 — AI 인간화 대규모 개편

- 서버 권위 시야각·수풀·마지막 목격·청각 오차
- 반응시간·회전속도·조준 오차·이동 사격 페널티·점사
- 재장전·후퇴·건물 탈출·단계별 갇힘 복구
- 기존 현장 채팅과 분리된 `aiDialogue` 말풍선 이벤트
- 기존 수동 무기·차량·늑대 시즌 밸런스 보존

## Refactor 020 — AI Vehicle, Supply & Werewolf Objective Rivalry

- AI can choose distant supply drops, useful loot, zone movement, patrol destinations, and the active werewolf altar as long-range objectives.
- Multiple AI may chase the same empty motorcycle; the first arrival mounts and the others re-plan after losing the race.
- AI motorcycles use server-authoritative vehicle input, obstacle probing, direction retention, reverse recovery, and safe dismount rules.
- AI dismounts near supply drops and loot, opens supply crates, collects nearby valuable contents, and can return to the motorcycle.
- AI can approach the active altar, complete the stationary ritual, receive the curse, and automatically transform.
- Transformed or cursed AI cannot continue using a motorcycle.
- Debug state changes are visible with `DROP8_AI_DEBUG=1` under `DROP8 AI VEHICLE`, `DROP8 AI OBJECTIVE`, and `DROP8 AI RIVALRY`.


## Refactor 020 FIXED v2 — AI Vehicle Objectives, Werewolf Predator & Adhesive Control

- Keeps the Refactor 020 AI motorcycle, supply-drop, altar, dismount, return and rivalry behavior.
- Buffs werewolf pursuit speed while leaving motorcycle tuning unchanged.
- Reduces ordinary bullet, explosion, melee, fire and vehicle damage received by transformed werewolves; zone damage remains normal and silver remains the dedicated counter.
- Adds a Sunfire-style werewolf proximity aura: nearby players take interval damage, motorcycles take damage, and sustained contact force-dismounts the rider.
- Adds three accumulating adhesive stages for players, AI and werewolves, with 72% / 55% / 38% movement multipliers and a gradual recovery tail instead of instant full-speed recovery.
- Strengthens motorcycle adhesive slowdown to 45% maximum speed with weaker acceleration and steering for a longer duration.
- Adds synchronized adhesive stage fields and client prediction/visual feedback.

## Refactor 021 — AI Persona Fast Dialogue

- Added seven fixed AI cast members: 준희커, 대성(빅생), 양정횬, 페이커, 케리아, 윤석10, 락승타.
- Replaced 손흥민 and the previous 대성 display name with the fixed cast names.
- Added 150+ unique lines per persona, signature phrases, nearby response combinations, recent-line memory, and a 40-second repeat guard.
- Existing Refactor 019 perception/combat behavior now selects persona-specific tactical lines without changing weapon, vehicle, werewolf, map, or player-chat balance.
- Added server-side casual chatter scheduling, staggered 0.4–1.5 second replies, and distance-limited AI voice delivery.
- Casual AI speech uses the existing overhead bubble but does not flood the left field-chat log; tactical messages remain loggable.
- Player chat input and Space key handling are unchanged.

## Refactor 022 — AI Navigation & Tactical Recovery

- AI 정체 판정을 단순 이동량이 아니라 waypoint/최종 목표 진척도로 변경했다.
- 최근 위치 이력으로 A-B-A-B 왕복과 좁은 반경 맴돌기를 감지한다.
- 수영 중에는 전투·파밍보다 shore exit 탈출을 우선하고 실패한 출구는 잠시 재선택하지 않는다.
- 강을 가로지르는 직선 경로를 탐지해 다리·여울을 우선하는 terrain-aware 경로를 구성한다.
- 전투 적정 거리에서 영구 정지하지 않고 무기별 간격으로 짧은 좌우 전술 이동을 수행한다.
- 막힌 사격 위치에서 측면 이동을 시도하고 반복 실패 시 해당 교전을 잠시 포기한다.
- 목표 잠금·우선순위·실패 목표 쿨다운을 추가해 짧은 주기의 목표 진동을 줄였다.
- AI 페르소나 대사, 플레이어 이동, 무기 수치, 차량·늑대인간·맵 가시성은 변경하지 않았다.
